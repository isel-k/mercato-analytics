name: "mercato_analytics"
version: "1.0.0"
config-version: 2

profile: "mercato_analytics"

model-paths: ["models"]
test-paths: ["tests"]
clean-targets: ["target", "dbt_packages"]

models:
  mercato_analytics:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: ephemeral
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts

seeds:
  mercato_analytics:
    +schema: staging
